上下文管理

目的：在有限的Token预算里面，保留最有价值的信息，丢掉可以牺牲的信息

实现手段：
1. 第二层：估算token，当上下文达到最大token的75%的时候，执行全量压缩，仅保留近3轮最新对话
    全量压缩：利用llm对对话进行摘要，包括用户对话，工具调用结果，模型思考内容，工具调用请求
    问题：压缩后，内存或者数据库中是否还保留原始信息？
2. 第一层：tool调用结果落盘
    当单个工具的结果超过阈值的时候把完整内容写入磁盘，只保留预览和文件路径，
    同时控制单条消息的所有工具结果合计，超过则依次按大小存盘，但依旧会保留最近一轮对话调用的工具